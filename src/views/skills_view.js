export function generateSkills() {
  const containers = document.querySelectorAll('.container');
  const background = containers[2];
  background.innerHTML += '<section class="skills" id="skills">skills</section>';
}
